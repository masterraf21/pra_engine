from .models import CriticalPath, CriticalPathResuilt


def compare_critical_path(baseline: list[CriticalPath],
                          realtime: list[CriticalPath]) -> CriticalPathResuilt:
    pass
